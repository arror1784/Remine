# 사내 백오피스 백엔드 & 설계 규율

> 스타트업 재무 관리(번레이트·런웨이) 백오피스의 서버 사이드. 무엇을 왜 이렇게 쓰는지, 그리고 큰 코드베이스를 규율로 지탱하는 방법 정리. 학생 멘토링 참고용.

한 줄 요약: **Kotlin + Spring Boot 기반, 헥사고날 아키텍처(Ports & Adapters) + CQRS, 40여 개 Gradle 멀티모듈.** DB는 PostgreSQL(운영)/H2(개발), 캐시·큐는 Redis, 스키마는 Flyway 마이그레이션으로만 바꾼다.

---

## 01. 백엔드 기술 스택

`build.gradle.kts` 실측 기준.

### 언어 · 프레임워크 · 런타임

| 항목 | 버전 | 역할 — 왜 |
|---|---|---|
| **Kotlin** | 1.9.22 | 주 언어. null 안전성 + 간결함. JPA/Spring 플러그인(allopen·noarg·kapt) 사용. |
| **Spring Boot** | 2.7.18 | 웹·DI·트랜잭션·시큐리티의 토대. |
| **JDK** | 17 | JVM 타깃. |
| **Gradle** (Kotlin DSL) | — | 멀티모듈 빌드. `bootJar`(앱)/`jar`(도메인) 분리. |

### 데이터 · 영속성

| 항목 | 역할 — 왜 |
|---|---|
| **PostgreSQL** 42.7 (운영) / **H2** (개발) | 관계형 DB. 개발은 인메모리로 가볍게. |
| **JPA / Hibernate** | ORM. 단, 외래키(FK) 제약은 걸지 않는다(뒤 규칙 참고). |
| **Flyway** (`migration` 모듈) | DB 스키마 버전 관리. 모든 DDL은 멱등(`IF NOT EXISTS`). 운영은 `ddl-auto=validate`. |
| **Redis** | 세션 저장 + 분산락 + Redis Stream 기반 작업 큐(`wim:task:*`). |

### 웹 · 보안 · 문서

| 항목 | 역할 — 왜 |
|---|---|
| **Spring Security** 5.7 | 인증/인가. JWT 필터 체인 + Google OAuth. |
| **springdoc-openapi** 1.7 | Swagger UI 자동 생성. 모든 DTO에 `@Schema` 문서화. |
| **Jackson** (kotlin module) | JSON 직렬화. |
| **Bean Validation** | 요청 검증. |

### 외부 연동 클라이언트 (전용 모듈로 격리)

`client-minio`(파일 스토리지) · `client-directsend`(이메일) · `client-granter`(회계 데이터 동기화) · `client-kb-service`(AI/지식 서비스 프록시) · `client-shiftee`(근태).

> **멘토링 포인트.** 외부 시스템 연동을 각각 `client-*` 모듈로 떼어낸다. 이유: 외부 API가 바뀌어도 그 모듈만 고치면 되고, 도메인 로직이 외부 SDK에 오염되지 않는다. "경계를 모듈로 강제"하는 대표적 예.

---

## 02. 아키텍처 — 헥사고날 + CQRS

### 멀티모듈 구조

40여 개 모듈. 크게 세 부류:

- **앱 모듈**(`bootJar`, 실제 실행): `app-api`(REST API 서버) · `app-worker`(스케줄러·백그라운드 워커).
- **도메인 모듈**(`jar`, 기능 단위): `employee` · `rent` · `equipment` · `othercost` · `revenue-contract` · `investment-round` · `corporate-loan` · `dashboard` · `simulation` · `attendance` · `leave` … 등. 재무·경영의 각 관심사를 한 모듈로.
- **인프라·공통**: `common`(공통) · `auth`/`auth-api`(인증) · `rbac`(권한) · `migration`(스키마) · `mvc-wrapper` · `client-*`(외부연동).

### 레이어 3층 (의존 방향 고정)

```
Adapter  →  Application  →  Domain
  ↑             ↑             ↑
외부 시스템    포트(계약)    순수 비즈니스 로직
```

- **Domain** — 순수 비즈니스 로직. 엔티티·값 객체·예외. **Spring/JPA/외부에 의존 금지.**
- **Application** — 유스케이스 조합. 인바운드 포트(Command/Query) + 아웃바운드 포트(외부 의존 인터페이스) + 서비스. `@Transactional` 경계도 여기.
- **Adapter** — 바깥과의 연결. REST 컨트롤러(presentation) + JPA 어댑터(infrastructure). 포트를 구현한다.

의존은 항상 안쪽(도메인)으로만 향한다. 그래서 DB나 웹 프레임워크를 바꿔도 도메인은 그대로다.

### CQRS — 명령과 조회 분리

상태를 바꾸는 것(Command)과 읽는 것(Query)을 인터페이스부터 나눈다. 각 Command/Query는 안에 `In`/`Out` 데이터 클래스를 둔다.

```kotlin
interface CreateEmployeeCommand {
    fun createEmployee(command: In): Out

    data class In(val name: String, val salary: Long, /* … */)
    data class Out(val entity: EmployeeDomainEntity)
}
```

### 표준 모듈 골격

```
{module}/src/main/kotlin/dev/wimcorp/{module}/
├── domain/          # *DomainEntity · vo · exception · event
├── application/
│   ├── service/     # *Service (유스케이스 구현)
│   ├── port/inbound/    # *Command · *Query
│   └── port/outbound/   # *OutboundPort
└── adapter/
    ├── presentation/web/    # 컨트롤러 + request/response DTO
    └── infrastructure/jpa/  # *JpaEntity · *RepositoryAdapter
```

---

## 03. 절대 규칙 (설계 방향 가드레일)

큰 코드베이스가 무너지지 않게 지키는 규칙. 대부분 **실제 사고에서 나왔다**.

1. **DB 외래키(FK) 제약 금지** — 관계는 ID 컬럼 + 인덱스로만. `@ManyToOne`으로 실제 FK를 걸지 않는다. 서비스 분리·마이그레이션 유연성 확보.
2. **AI/지식 처리를 Kotlin에서 재구현 금지** — 검색·임베딩·RAG·STT는 전부 Python 서비스(`kb-service`)가 한다. 백엔드는 `/api/v1/kb/**` 프록시로 위임만.
3. **설정·시크릿은 Vault로** — `application.yml`/코드 하드코딩 금지. `${ENV}` 주입, 부팅 시 로드. 기능 제거 시 그것을 먹이던 설정(env·프로퍼티 클래스·Vault 키)까지 끝까지 따라가 지운다.
4. **DB 스키마는 Flyway로만** — 운영 `ddl-auto=validate`. 엔티티만 고치고 마이그레이션을 안 만들면 부팅이 깨진다. 모든 DDL은 멱등.
5. **Redis Stream은 Python 서비스와 공유 계약** — 스트림 키·태스크 타입·필드를 양쪽 일치시킨다. 한쪽만 바꾸지 않는다.
6. **파일당 클래스 하나** (Command/Query의 `In`/`Out` inner만 예외). 패키지 두 단어는 언더스코어(`app_api`).
7. **헥사고날 의존 방향 고정** — `adapter → application → domain`. 컨트롤러에 DTO 인라인 정의 금지(별도 파일).
8. **인가는 `@PreAuthorize`로만** — 필터에 URL 경로 하드코딩 금지. (봇 전원이 403 나던 실제 사고에서 나온 규칙.)
9. **목록 일괄 저장은 bulk `sync` 제공** — 프론트가 행마다 N번 호출하게 두지 말고 `PATCH /<리소스>/sync` 한 방(findAllByIds + saveAll 단일 트랜잭션). 삭제는 sync에 싣지 않는다.
10. 🚨 **새 엔티티는 무조건 `BaseOrmEntity` 상속** — `id`·`created_at`·`updated_at`·`deleted_at`(소프트삭제) 3종을 공통 제공. `created_at`을 손으로 다시 선언하면 십중팔구 `deleted_at`을 빠뜨린다. **"삭제"를 `is_active=false`로 얹지 말 것**(둘은 의미가 다르다). 이 규칙을 어긴 카탈로그 7개가 "관리자 화면엔 뜨는데 드롭다운엔 안 뜨는" 유령 데이터 사고를 냈다.

### HTTP 메서드 규칙
생성=`POST` / **수정=반드시 `PATCH`**(`PUT` 금지) / 삭제=`DELETE` / 조회=`GET`.

> **멘토링 포인트.** 규칙마다 "왜"와 "어떤 사고에서 나왔는지"가 붙어 있다. 좋은 팀 규칙은 추상적 원칙이 아니라 **아팠던 경험의 응축**이다. 규칙 8·9·10 모두 실제 버그 → 재발 방지 문서화의 결과.

---

## 04. 빌드 · 테스트

```bash
./gradlew build                         # 전체 빌드
./gradlew :app-api:build                # 특정 모듈만
./gradlew build -x test                 # 테스트 제외 빌드
./gradlew :employee:test                # 모듈 테스트
./gradlew :employee:test --tests "EmployeeServiceTest"   # 단일 테스트
./gradlew :app-api:bootRun              # 앱 실행
```

---

## 05. 하네스 — AI를 큰 코드베이스에 태우는 골격

"하네스(harness)"란 AI 코딩 도구가 저장소 규칙·워크플로 안에서 안전하게 굴러가도록 감싸는 틀. 40개 모듈 규모에서 특히 중요하다 — AI가 규칙을 모르면 헥사고날 경계를 넘고 FK를 걸어버린다.

**Layer 1 — 계층형 규칙 로딩**
전역 → 저장소 루트 `CLAUDE.md`(시스템 전역 불변식) → `backend/CLAUDE.md`(절대 규칙 10개, 항상 로드).

⬇

**Layer 2 — 온디맨드 상세 규칙**
`backend/.claude/rules/*.md`(architecture · module-structure · conventions · domain-modules · build-test)는 자동 로드되지 않는다. 해당 작업을 할 때만 읽어 컨텍스트를 아낀다. "헥사고날 이해가 필요하면 architecture.md를, 마이그레이션 SQL을 쓰면 module-structure.md를 먼저 읽어라"가 문서에 명시돼 있다.

⬇

**Layer 3 — 런북 스킬**
`.claude/skills/`에 배포(release)·긴급패치(hotfix)·DB 계정 관리(db-account-admin)·운영 접속(teleport-access) 절차를 단계별 문서로. AI가 "릴리스해줘"를 받으면 이 런북을 순서대로 실행 — 사람이 하던 체크리스트의 코드화.

⬇

**Layer 4 — 워크트리 격리 + PR 게이트**
새 작업은 격리된 작업 공간(`.worktrees/<branch>`)에서. `main`·`staging`엔 직접 커밋하지 않고 항상 **브랜치 → PR**. `feature → staging` 검증 후 `staging → main`(실배포) 승격은 **사람이 승인**한다.

⬇

**Layer 5 — 검증 문화**
"완료"라고 말하기 전에 실제로 돌려 확인. 스테이징 배포 후 개발용 토큰으로 API를 직접 호출해 응답을 검증 — 테스트 통과만으로 끝내지 않는다.

> **핵심 발상.** AI에게 자유를 다 주는 게 아니라, **사람이 지키던 규칙·절차·게이트를 문서로 만들어 그 안에서 일하게** 한다. 모듈이 많을수록 "짧은 상시 규칙 + 필요할 때 읽는 깊은 규칙"의 온디맨드 로딩이 토큰과 정확도 양쪽에서 이득이다.

```
~/.claude/CLAUDE.md          # 전역: git·워크트리·문서 스타일
CLAUDE.md                    # 루트: 시스템 전역 불변식(FK 금지·Vault·API 경계)
backend/CLAUDE.md            # 백엔드: 절대 규칙 10개 (항상 로드)
backend/.claude/rules/       # 상세: 헥사고날·모듈·컨벤션·마이그레이션 (필요할 때)
backend/.claude/skills/      # 런북: release / hotfix / db-account / teleport
```

---

*실무 백오피스 프로젝트에서 실제 사용 중인 구성 정리 · 학생 멘토링 참고용*
*Kotlin · Spring Boot · Hexagonal · CQRS · PostgreSQL · Redis · Flyway · Claude Code*
